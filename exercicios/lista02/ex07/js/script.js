function sortearDado() {
    let numeroSorteado = Math.floor(Math.random() * 6) + 1;

    document.write(`O número sorteado foi: ${numeroSorteado}`);
}
sortearDado();
// nesse exercicio era pra ter implementado imagens. Mas não consegui a tempo.
